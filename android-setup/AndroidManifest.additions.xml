<!--
  MERGE THESE INTO  android/app/src/main/AndroidManifest.xml
  (generated by `npx cap add android`). Do not replace the whole file — add the
  permissions inside <manifest>, and the intent-filter inside the existing
  <activity android:name=".MainActivity"> element.
-->

<!-- ===== PERMISSIONS (add as children of <manifest>, before <application>) ===== -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<!-- For the fast-follow native call experience (foreground service while in a call): -->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
<!-- Declare hardware as optional so phones without a camera can still install: -->
<uses-feature android:name="android.hardware.camera" android:required="false" />
<uses-feature android:name="android.hardware.microphone" android:required="false" />

<!-- ===== APP LINKS (add INSIDE <activity android:name=".MainActivity">) =====
     android:autoVerify="true" makes Android verify ownership against
     https://enovoapp.com/.well-known/assetlinks.json (already in public/). -->
<intent-filter android:autoVerify="true">
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />
    <data android:scheme="https" android:host="enovoapp.com" />
    <data android:scheme="https" android:host="www.enovoapp.com" />
</intent-filter>
